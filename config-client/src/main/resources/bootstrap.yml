server:
  port: 8889
spring:
  application:
    name: configclient
  cloud:
    config:
      profile: test
      uri: http://localhost:8888/
      label: master
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:9010/eureka/